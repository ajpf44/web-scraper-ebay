{
	"name": "trainee-challenge",
	"module": "index.ts",
	"type": "module",
	"private": true,
	"devDependencies": {
		"@types/bun": "latest"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"dependencies": {
		"axios": "^1.8.4",
		"concurrently": "^9.1.2",
		"express": "^4.21.2",
		"jsdom": "^26.0.0"
	},
	"scripts": {
		"install": "concurrently \"cd back && bun install \" \"cd front && bun install \"",
		"start": "concurrently \"cd back && bun install &&bun index.ts\" \"cd front && bun install && bun run dev\""
	}
}
